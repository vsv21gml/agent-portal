replicaCount: 1

image:
  repository: agent-portal-web
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: true
  className: alb
  host: agent-portal.local
  path: /
  annotations: {}
  tls: []

backend:
  servicePort: 4000

env: {}
