replicaCount: 1

image:
  repository: agent-portal-admin-web
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3100

ingress:
  enabled: true
  className: alb
  host: admin.agent-portal.local
  path: /
  annotations: {}
  tls: []

backend:
  servicePort: 4000

env: {}
