apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8s-api.fullname" . }}
  labels:
    {{- include "k8s-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "k8s-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
